services:
  db:
    image: postgres:15
    container_name: savedata_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: savedata_development
      TZ: Asia/Tokyo
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    command: bash -c "bundle install && bundle exec rails db:prepare && rm -f tmp/pids/server.pid && bundle exec foreman start -f Procfile.dev"
    working_dir: /app
    volumes:
      - .:/app
      - bundle_data:/usr/local/bundle
    ports:
      - "3000:3000"
    environment:
      - RAILS_ENV=development
      - DATABASE_URL=postgres://postgres:password@db:5432/savedata_development
      - TZ=Asia/Tokyo
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy

  ai_service:
    build: ../savedata_ai
    ports:
      - "8000:8000"
    env_file:
      - ../savedata_ai/.env

volumes:
  postgres_data:
  bundle_data: