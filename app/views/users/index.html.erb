<% content_for :title, "ユーザー一覧 - Savedata" %>

<div class="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-7xl mx-auto">
    <!-- ヘッダーセクション -->
    <div class="mb-12">
      <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
        ユーザー一覧
      </h1>
      <p class="text-lg text-gray-600 max-w-2xl">
        システムに登録されているユーザーの一覧です。ユーザー情報の確認・編集が行えます。
      </p>
    </div>

    <!-- ノーティス -->
    <% if notice %>
      <div class="mb-6 p-4 bg-green-50 border border-green-200 rounded-lg">
        <p class="text-green-800 font-medium"><%= notice %></p>
      </div>
    <% end %>

    <!-- アクションボタン -->
    <div class="mb-8">
      <%= link_to "新規ユーザー登録", new_user_path, class: "inline-block px-6 py-3 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition-colors" %>
    </div>

    <!-- ユーザーグリッド -->
    <div id="users" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @users.each do |user| %>
        <%= render "user_card", user: user %>
      <% end %>
    </div>

    <!-- ユーザーが存在しない場合 -->
    <% if @users.empty? %>
      <div class="text-center py-12">
        <p class="text-gray-500 text-lg mb-4">登録されているユーザーはありません</p>
        <%= link_to "最初のユーザーを登録", new_user_path, class: "inline-block px-6 py-3 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition-colors" %>
      </div>
    <% end %>
  </div>
</div>
