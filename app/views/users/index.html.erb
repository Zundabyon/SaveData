<p style="color: green"><%= notice %></p>

<% content_for :title, "Users" %>

<h1>ユーザー一覧</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to "ユーザーの表示", user %>
    </p>
  <% end %>
</div>

<%= link_to "新規ユーザー登録", new_user_path %>
